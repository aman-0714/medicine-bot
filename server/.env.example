# Anthropic API key — get one at https://console.anthropic.com
# This file stays on the server only. Never commit it, never ship it in the app.
ANTHROPIC_API_KEY=your_key_here

# Port the server listens on
PORT=3000
